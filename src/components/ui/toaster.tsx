
// This file is kept for compatibility with any code that might import it,
// but we're using sonner's Toaster component directly in App.tsx instead.
import * as React from "react"

export function Toaster() {
  // This is a no-op component that doesn't render anything
  // All toast functionality is provided by sonner's Toaster in App.tsx
  return null;
}
